# Core ML — Keras with PyTorch backend
keras>=3.0.0

# NOTE: PyTorch CPU is installed separately in render.yaml buildCommand
# to force CPU-only wheels (avoids CUDA packages which are too large for free tier)

# Image processing
numpy>=1.21.0,<2.0.0
opencv-python-headless>=4.5.0
matplotlib>=3.5.0
pandas>=2.0.0
openpyxl>=3.0.0

# API server
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Model hosting
huggingface-hub>=0.22.0
